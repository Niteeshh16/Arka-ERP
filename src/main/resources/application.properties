spring.application.name=Arka-ERP

# --- Database Configuration ---
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d716dn3uibrs739ok9ng-a:5432/arkadb_9ojj}
spring.datasource.username=${DB_USERNAME:arkadb_9ojj_user}
spring.datasource.password=${DB_PASSWORD:2B8CVo6N4EgPefyL2ttifk2miNcDO4fr}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Hibernate / JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true

# --- SQL Initialization ---
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
